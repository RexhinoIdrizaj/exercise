import { Box, Container } from "@mui/material";
import { UIList } from "../components";

const ScreenMain = () => {
  return (
    <Container>
      <UIList />
      <UIList />
    </Container>
  );
};

export default ScreenMain;
